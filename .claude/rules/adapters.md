---
paths:
  - "packages/adapters/**"
---

Ring 3. Imports @base/application, @base/domain, @base/contracts, zod. Never next, react, drizzle, supabase, hono. Controllers return outcome kinds, not HTTP codes. Presenters are pure functions producing view models of strings and booleans. Full rules: docs/layers/adapters.md
