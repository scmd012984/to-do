---
paths:
  - "packages/application/**"
---

Ring 2. Imports @base/domain only. Use cases, ports, request and response models, authorization through the permissions port. No strings for humans, no locale, no framework types. Every use case tested against memory ports. Full rules: docs/layers/application.md
